int singleNumber(int* nums, int numsSize) {
    int num = 0;
    for(int i = 0; i < numsSize; i++) {
        num ^= nums[i];  // XOR operation
    }
    return num;
}

Example 1:
Input: nums = [2,2,1]

Output: 1
